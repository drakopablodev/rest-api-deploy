/**
 * Constant variables of the program
 */
module.exports = Object.freeze({
  DEFAULT_PORT: process.env.PORT ?? 3000 // Variable de entorno del proceso o 3000 por defecto (el servicio de hosting te dice el puerto de esa forma)
})
